// Tweet Character Counter 
var Count=prompt("write here");
var totalChar = 240;
var reaminingChar= totalChar - Count.length;

alert("you have written " + Count.length + " characters and now left " + reaminingChar);
